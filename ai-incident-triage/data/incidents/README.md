# data/incidents

The four JSON files in this directory are placeholders pending real mock data:

- `crashloopbackoff.json`
- `imagepullbackoff.json`
- `http503.json`
- `database_timeout.json`

Each currently contains a minimal valid `{}` so nothing breaks. Author real mock
incidents here when ready.
